# Environment / secrets
.env
.env.*
!.env.example

# Logs
logs/
*.log
scripts/.pids/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
agecob-lens/dist/
agecob-lens/.vite/

# Local tooling binaries (caddy.exe, etc) + Caddy local CA/state
.tools/

# IDE / local tooling
.cursor/
.vscode/
.idea/
.vs/

# Local agent/scratch state
.deepseek/
agecob-lens/docs/aa.txt

# Relatórios exportados (podem conter PII — CPF completo). Não versionar.
relatorio_*.csv
relatorio-carteiras-*.csv
relatorio-carteiras-*.csv.txt

# Claude Code local state
.claude/settings.local.json

# ML — raw training data (não versionar; baixar do repo KNN-agecob quando precisar retreinar)
deploy/d.csv

# graphify artifacts (generated)
graphify-out/
.graphify_*

# Metas extraídas de PDF (ETL)
dados_metas/

# PDFs de metas — contêm PII (CPF, DocuSign Envelope ID). Não versionar.
docs/*.pdf

# design-sync (claude.ai/design) — staged scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
